name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    uses: kubixhq/.github/.github/workflows/reusable-release.yml@main
    with:
      image-name: kubix-schema
      language: go
