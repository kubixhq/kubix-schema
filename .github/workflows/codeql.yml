name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 9 * * 1"

jobs:
  codeql:
    uses: kubixhq/.github/.github/workflows/reusable-codeql.yml@main
    with:
      language: go
